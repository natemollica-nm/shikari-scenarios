## Maintaining Fork Repository

**Pushing local repo commits to fork**

```shell
$ git push fork main
```

**Fetch and merge updates from upstream (main repo)**

```shell
$ git fetch upstream
```

```shell
$ git merge upstream/main
```

